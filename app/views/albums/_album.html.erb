

  <% poster_url = album ? album.image_url : 'add_new.png' %>
<% path = album ? album_path(album.id) : new_album_path %>

<div class="album-box">
<ul>
<%= link_to image_tag(poster_url), path%>
  <% if album %>
    <h4><%= link_to album.title, album_path(album.id) %></h4>
  <% end %>
</ul>
</div>
